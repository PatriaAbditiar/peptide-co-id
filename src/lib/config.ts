/**
 * Feature flags for Peptide.co.id
 * Toggle features on/off without removing code.
 */

export const SITE_CONFIG = {
  /** Show seller directory and "where to buy" sections across the site. */
  SHOW_SELLERS: false,
};
